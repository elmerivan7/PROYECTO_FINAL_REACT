import React from "react";

import MenuNav from "./MenuNav";

export default {
  title: "Components/MenuNav",
  component: MenuNav,
  argTypes: {},
};

const Template = (args) => <MenuNav {...args} />;

export const Type = Template.bind({});
Type.args = {};
